# SQL Database Configuration
SQL_DATABASE_URL=postgresql://user:password@localhost:5432/recommendation_db

# MongoDB Configuration (for purchase history & conversation history)
MONGO_URI=mongodb://localhost:27017/
MONGO_DATABASE=recommendation_db

# Redis Configuration (for short-term session memory)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# Memory Management Configuration
SESSION_TTL=3600  # Session timeout in seconds (default: 1 hour)
MAX_CONTEXT_MESSAGES=10  # Maximum messages to keep in context

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
